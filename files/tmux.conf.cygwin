
# copy selection to system clipboard
unbind y
unbind Enter
bind -t vi-copy y copy-pipe "cat | clip"
bind -t vi-copy Enter copy-pipe "cat | clip"
bind > run '( tmux show-buffer | cat | clip ) && tmux display-message \"buffer copied\"'
bind < run '~/bin/paste.exe | tmux load-buffer - ; tmux paste-buffer'

