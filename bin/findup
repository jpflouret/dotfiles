#!/bin/bash
slashes=${PWD//[^\/]/}
directory="$PWD"
for (( n=${#slashes}; n>0; --n ))
do
    if [ -e "$directory/$1" ]
    then
        readlink -f "$directory/$1"
        exit 0
    fi
    directory="$directory/.."
done
exit 1
